name: "快问快答 (软件开发类)"
description: "开发中遇到问题？请使用此模板提问，帮助我们更快地帮助你。"
title: "[问题类型]：请在这里写一个清晰的标题"
labels: ["Q&A"]
body:
  - type: markdown
    attributes:
      value: |
        **感谢你花时间填写这个模板！提供完整信息有助于我们更快地定位和解决问题。**

  - type: textarea
    id: problem-context
    attributes:
      label: "你想解决什么问题？"
      description: "请用简明扼要的语言描述你的目标或遇到的问题。说明你正在做什么，期望的结果是什么，以及实际发生了什么。"
      placeholder: "例如：我正在使用 Python 的 requests 库向某个需要认证的 API 发送 POST 请求，期望返回 200 状态码，但服务器总是返回 HTTP 403 Forbidden。"
    validations:
      required: true

  - type: textarea
    id: code-or-error
    attributes:
      label: "相关代码片段和完整的错误信息"
      description: "请提供能复现问题的最小代码片段，以及完整的错误堆栈信息。使用 ``` 代码块包裹。"
      render: markdown
      placeholder: |
        ```python
        # 关键代码
        import requests

        headers = {"Authorization": "Bearer YOUR_TOKEN"} # 关键点：我添加了认证头
        url = "https://api.example.com/data"
        data = {"key": "value"}

        response = requests.post(url, json=data, headers=headers)
        print(response.status_code) 
        print(response.text)
        ```

        ```
        # 完整的错误日志
        HTTPError: 403 Client Error: Forbidden for url: https://api.example.com/data
        {"detail":"Authentication credentials were not provided or were invalid."}
        ```
    validations:
      required: true # <--- 建议将此项也设为必填

  - type: textarea
    id: what-tried
    attributes:
      label: "你已尝试过哪些方法？"
      description: "列出你已经做过的排查步骤和搜索过的关键词。这可以避免佬友们提出你已经试过的方案。"
      placeholder: |
        1. 我已经查阅了 API 文档，确认 `Authorization` Header 是必需的。
        2. 我使用 Postman 使用相同的 URL、Header 和 Body 测试，请求是成功的。
        3. 我确认了代码中的 URL 和 Token 没有拼写错误。
        4. 我搜索了“python requests 403 forbidden postman works”。
    validations:
      required: true

  - type: markdown
    attributes:
      value: "## 🛠️ 环境信息 (Environment)"

  - type: checkboxes # <--- 优化点2：dropdown -> checkboxes
    id: tech-stack
    attributes:
      label: "主要技术栈 (可多选)"
      description: "请选择问题相关的主要技术栈。"
      options:
        - label: Python
        - label: JavaScript/Node.js
        - label: Java
        - label: Go
        - label: PHP
        - label: Rust
        - label: Linux/Shell
        - label: 数据库 (MySQL/PostgreSQL等)
        - label: 前端框架 (React/Vue/Angular等)
        - label: Docker/Kubernetes
        - label: 其他（请在下方说明）
    validations:
      required: true

  - type: textarea
    id: additional-environment
    attributes:
      label: "版本号和其他环境信息"
      description: "请提供主要技术栈的具体版本号，以及操作系统、浏览器、相关库版本等任何有助于诊断的信息。"
      render: markdown
      placeholder: |
        - **OS**: Ubuntu 22.04
        - **Python**: 3.10.6
        - **requests**: 2.28.1
        - **其他**: 我是在一个 Docker 容器内运行这段代码的。
    validations:
      required: true

  - type: markdown # <--- 优化点3：增加视觉辅助提示
    attributes:
      value: |
        **提示**: 如果问题涉及用户界面(UI)，强烈建议附上截图或GIF动图来直观地展示问题。你可以直接将图片拖拽到文本框中上传。

  - type: checkboxes # <--- 优化点4：增加最终检查清单
    id: final-checklist
    attributes:
      label: "提交前检查"
      description: "在提交之前，请确认你已经完成了以下步骤。"
      options:
        - label: 我已经搜索过 L 站，确认没有重复提问。
          required: true
        - label: 我已经提供了清晰的标题来概括问题。
          required: true
        - label: 我已经提供了复现问题所需的所有信息。
          required: true
        - label: 我已经仔细阅读并填写了模板中的所有必填项。
          required: true